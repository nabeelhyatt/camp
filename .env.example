# Camp Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# Default OpenRouter API key for new users (optional)
# If set, new users will have this key pre-configured and skip onboarding
# VITE_DEFAULT_OPENROUTER_KEY=sk-or-v1-your-key-here

# Backend selection (optional, defaults to "chorus")
# Options: "chorus" (uses app.chorus.sh) or "camp" (uses app.getcamp.ai)
# VITE_CAMP_BACKEND=chorus

# ============================================
# Multiplayer Configuration (Phase 1)
# ============================================

# Enable multiplayer features (requires Convex + Clerk setup)
# VITE_MULTIPLAYER_ENABLED=false

# Convex deployment URL
# Get this from your Convex dashboard after running `npx convex dev`
# VITE_CONVEX_URL=https://your-project.convex.cloud

# Clerk publishable key
# Get this from your Clerk dashboard (https://dashboard.clerk.com)
# VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx
